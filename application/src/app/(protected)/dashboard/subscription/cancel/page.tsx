import Cancel from 'components/dashboard/subscription/Cancel';

/**
 * Subscription cancellation page
 * @returns Page with subscription cancellation UI
 */
export default function CancelPage() {
  return <Cancel />;
}
